export default function Settings() {
    return <div className="p-10 text-center text-white">Settings Page</div>;
    // Create stuff like restart buttons, update functionaliity and more
    // For now we can have it "update from USB", connecting USB and pressing
    // the button copies contents to /root/www
}
